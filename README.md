Definimos la función de error: 

$$
E(x) = \sum_{i=1}^n (y_i - x_{i1} \beta_{i1} + x_{i2} \beta_{i2} + ... + x_{ip} \beta_{ip})^2
$$ 

Utilizando notación matricia. Podemos ver $$Y$$ como una matriz con vectores columna $$y_i$$ que contienen las respuestas escalares, X como una matriz con vectores columna Xi que contienen a los regresores y a $$\beta$$ como una matriz con vectores columna $$\beta_i$$ que contienen a los parametros.

$$
\begin{align*}
\implies & E(x) = 

Normal equations:  


